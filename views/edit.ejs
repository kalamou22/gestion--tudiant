<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <title>Modifier un étudiant</title>
  <style>
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background: linear-gradient(135deg, #74ebd5 0%, #ACB6E5 100%);
      display: flex;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
      margin: 0;
    }

    .form-box {
      background-color: #fff;
      padding: 30px 40px;
      border-radius: 12px;
      box-shadow: 0 0 20px rgba(0, 0, 0, 0.1);
      width: 100%;
      max-width: 600px;
    }

    h2 {
      text-align: center;
      margin-bottom: 25px;
      color: #333;
    }

    form input, form select {
      width: 100%;
      padding: 12px;
      margin-bottom: 15px;
      border: 1px solid #ccc;
      border-radius: 6px;
      font-size: 16px;
    }

    form button {
      width: 100%;
      padding: 12px;
      background-color: #5c6bc0;
      color: white;
      border: none;
      border-radius: 6px;
      font-size: 16px;
      cursor: pointer;
      transition: background-color 0.3s ease;
    }

    form button:hover {
      background-color: #3f51b5;
    }

    .back-link {
      display: block;
      text-align: center;
      margin-top: 20px;
      color: #333;
      text-decoration: none;
    }

    .back-link:hover {
      text-decoration: underline;
    }

    .readonly-field {
      background-color: #eee;
      padding: 12px;
      border-radius: 6px;
      margin-bottom: 15px;
      border: 1px solid #ccc;
    }
  </style>
</head>
<body>
  <div class="form-box">
    <h2>Modifier un étudiant</h2>

    <form action="/etudiants/modifier/<%= etudiant.matricule %>" method="POST">
      <div class="readonly-field"><strong>Matricule :</strong> <%= etudiant.matricule %></div>
      <input type="hidden" name="matricule" value="<%= etudiant.matricule %>">

      <input type="text" name="nom" placeholder="Nom" value="<%= etudiant.nom %>" required>
      <input type="text" name="prenom" placeholder="Prénom" value="<%= etudiant.prenom %>" required>
      <input type="date" name="date_naissance" value="<%= new Date(etudiant.date_naissance).toISOString().split('T')[0] %>" required>
      <input type="text" name="adresse" placeholder="Adresse" value="<%= etudiant.adresse %>" required>
      <input type="text" name="sexe" placeholder="Sexe (H/F)" value="<%= etudiant.sexe %>" required>
      <input type="text" name="nationalite" placeholder="Nationalité" value="<%= etudiant.nationalite %>" required>
      <input type="text" name="universite" placeholder="Université" value="<%= etudiant.universite %>" required>
      <input type="text" name="classe" placeholder="Classe" value="<%= etudiant.classe %>" required>
      <input type="text" name="filiere" placeholder="Filière" value="<%= etudiant.filiere %>" required>

      <button type="submit">Enregistrer les modifications</button>
    </form>

    <a href="/etudiants" class="back-link">← Retour à la liste des étudiants</a>
  </div>
</body>
</html>
